---
# Default infrastructure configuration
# This file only specifies the default provider selection
# Complete provider configurations are in stack/infra/providers/{provider}/values.yaml

# Default provider (used by 'klabctl init')
provider: proxmox
